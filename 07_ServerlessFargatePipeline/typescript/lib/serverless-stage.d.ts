import { Stage, StageProps } from 'aws-cdk-lib';
import { Construct } from 'constructs';
export declare class ServerlessStage extends Stage {
    constructor(scope: Construct, id: string, props?: StageProps);
}
